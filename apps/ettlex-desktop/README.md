# EttleX Desktop

Planned stack:

- Tauri (Rust backend)
- SvelteKit (TypeScript frontend)

Initialize when ready:

1. npm create svelte@latest
2. tauri init
3. wire src-tauri to crates/ettlex-tauri (or copy minimal main.rs + invoke handlers)
