[package]
name = "ettlex-agent-api"
version = "0.1.0"
edition = "2021"

[lib]
name = "ettlex_agent_api"
path = "src/lib.rs"

[dependencies]
ettlex-memory = { path = "../ettlex-memory" }

[dev-dependencies]
