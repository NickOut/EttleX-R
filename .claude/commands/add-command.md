# Add Custom Command

Create a new project slash command and save it to the project's .claude/commands directory.

## Command Content

$ARGUMENTS

## Instructions

Take the provided command content and:

1. Generate a filename
2. Save it in `./.claude/commands/` (project directory, not home directory)
3. It'll be available as a project command /filename (drop the .md)
